import { tv } from "tailwind-variants";

const InputStyle = tv({
  base: "block min-h-[auto] text-black w-full rounded border border-black-500 bg-transparent px-3 py-[0.32rem] leading-[2.15] outline-none transition-all duration-200 ease-linear",
});

export default InputStyle;
